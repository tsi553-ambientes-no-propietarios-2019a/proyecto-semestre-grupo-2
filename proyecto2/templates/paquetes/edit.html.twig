{% extends 'base.html.twig' %}

{% block title %}Edit Paquetes{% endblock %}

{% block body %}
    <h1>Edit Paquetes</h1>

    {{ include('paquetes/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('paquetes_index') }}">back to list</a>

    {{ include('paquetes/_delete_form.html.twig') }}
{% endblock %}
